import { wp } from "app/resources/config";
import { StyleSheet } from "react-native";

const hmScrollViewStyles = () =>
  StyleSheet.create({
    container: { 
      //paddingHorizontal: wp(16), 
      flex: 1 
    },
  });

export default hmScrollViewStyles;
